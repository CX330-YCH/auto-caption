# Auto Caption 项目级约束

## 1. 适用范围与优先级

- 本文件位于仓库根目录，约束整个仓库及其所有子目录。
- 用户在当前任务中的明确要求优先于本文件；若两者冲突，先指出冲突，再遵循用户的最新明确要求。
- 子目录如存在更具体的 `AGENTS.md`，仅能补充该子目录规则，不得降低本文件的安全、记录、兼容性和验证要求。
- 本文件中的“必须”“禁止”是强制要求；“建议”可以在有充分理由时调整，但理由必须记录在根目录 `change.md`。

## 2. 项目目标与当前边界

Auto Caption 是一个跨平台实时字幕桌面应用，主要由以下部分组成：

- Electron 主进程：窗口、配置、Python 子进程、IPC 和生命周期管理。
- Vue 3 渲染进程：控制窗口、字幕窗口、Pinia 状态与多语言界面。
- Python 字幕引擎：音频采集、预处理、语音识别、翻译及引擎协议。
- 打包链路：`electron-builder` 打包桌面应用，PyInstaller 打包 Python 引擎。

项目支持 Windows、macOS 和 Linux，但当前发布和回归验证以 Windows 为主要平台。任何修改不得无说明地破坏其他平台。

## 3. 授权边界

### 3.1 默认只读

- 当用户要求“分析”“评估”“了解”“审查”“解释”“规划”“给方案”或询问“能否实现”时，默认只允许读取、分析和报告，不得修改文件。
- “能否添加某功能”“应该怎样实现”不等于授权实现。
- 只有用户明确要求“修改”“实现”“新增”“修复”“重构”“生成并写入文件”等操作时，才可以写入仓库。
- 用户限定“只做技术评估”“不要动代码”时，禁止以任何理由写入代码、配置、文档或生成文件。

### 3.2 不自动扩大范围

- 不得把一个功能请求自动扩大为大规模重构、依赖升级、格式化全仓库、构建发布或远端资源操作。
- 如果可靠实现确实依赖前置重构，应先说明原因、范围、风险和阶段划分；未经授权不得开始前置重构。
- 不得因为“顺手”修复无关问题。发现无关问题时，只记录并报告。

### 3.3 外部和破坏性操作

- 未经明确授权，不得安装依赖、升级依赖、访问付费 API、创建或删除云端热词表、发布 Release、推送分支、创建 PR 或提交 Git commit。
- 不得使用 `git reset --hard`、`git checkout --`、递归删除等方式处理用户改动。
- 回退时只能回退本次任务中由当前执行者明确产生的修改，并在操作前确认精确文件范围。

## 4. 修改前必须执行的检查

每次获准修改前，必须完成以下步骤：

1. 阅读根目录和目标子目录适用的全部 `AGENTS.md`。
2. 执行 `git status --short --branch`，识别用户已有修改。
3. 阅读目标代码、相关类型、配置、测试和文档，不得只改表面调用点。
4. 明确本次授权范围、非目标和可能影响的兼容面。
5. 将工作拆成可独立验证、可独立回滚的最小阶段。
6. 规划本次 `change.md` 记录内容。

如果工作区已有用户修改：

- 必须保留并绕开无关修改。
- 修改同一文件前必须先阅读现有 diff。
- 无法安全区分时停止修改并向用户说明。

## 5. `change.md` 强制记录制度

### 5.1 基本规则

- 根目录 `change.md` 是本项目所有文件变更的完整流水记录。
- 每次修改仓库文件，都必须在同一变更批次中追加一条 `change.md` 记录。
- `change.md` 必须与对应代码或文档修改一起交付，不得事后补写。
- 只能追加历史，不得覆盖、重写或删除既有记录；纠错应追加“更正”记录并引用原条目。
- 纯只读分析没有文件变化时，不写空记录。
- 仅修改 `change.md` 本身也属于文件修改，必须说明修改原因；对同一批次记录的完善不需要递归新增另一条记录。

### 5.2 每条记录的必填内容

每条记录至少包含：

- 日期和标题。
- 用户授权与变更目标。
- 变更类型：功能、修复、重构、文档、测试、构建或配置。
- 修改文件清单，以及每个文件的具体修改原因。
- 修改前行为和修改后行为。
- 配置、IPC、子进程协议、命令行、数据结构是否变化。
- 兼容性、配置迁移和回滚方式。
- 实际执行的验证命令与真实结果。
- 未执行的验证及原因。
- 已知风险、限制和后续事项。
- 关键外部文档或技术决策来源。

### 5.3 禁止模糊记录

以下记录不合格：

- “修复了一些问题”。
- “优化代码”。
- “测试通过”但不写测试命令。
- 不列出修改文件。
- 隐瞒失败测试或把未运行写成通过。

## 6. 变更组织原则

- 重构、功能新增、全局格式化和依赖升级应分开进行，不得混成一个难以审查的变更。
- 每个阶段结束时仓库都应保持可理解、可验证、可回滚。
- 优先渐进式迁移，不进行没有测试保护的大爆炸式重写。
- 新抽象必须先解决明确的重复、耦合或扩展问题；禁止为了“看起来高级”引入无使用者的层级。
- 如果一个功能需要临时兼容层，应明确兼容层的删除条件，不得永久遗留双实现。

## 7. 禁止堆叠式实现

禁止通过以下方式继续堆积技术债：

- 在 `engine/main.py` 中为每个新引擎继续复制完整流程并无限增加 `if/elif`。
- 在 `src/main/utils/CaptionEngine.ts` 中继续集中所有路径拼装、参数生成、进程管理、协议解析和状态管理。
- 在 `EngineControl.vue` 中为每个引擎堆叠大量 `v-if="currentEngine === ..."`。
- 在扁平 `Controls` 接口中无边界追加所有供应商专属字段。
- 在不同识别引擎中复制音频队列循环、翻译线程、字幕事件构造和错误处理。
- 在主进程和渲染进程分别维护容易漂移的重复类型和默认配置。
- 通过复制旧代码后改名称的方式接入新 Provider。

如果当前结构不适合新增功能，应先提出最小必要重构方案，并将“无行为重构”和“功能接入”分阶段实施。

## 8. 目标架构约束

### 8.1 Python 字幕引擎

推荐逐步形成以下职责边界：

- `AudioSource`：只负责平台音频设备和原始帧读取。
- `AudioPipeline`：负责声道合并、采样率、帧长和 PCM 格式。
- `RecognitionProvider`：只负责特定 ASR 服务或本地模型。
- `RecognitionSession`：负责队列、生命周期、Provider 调度和统一事件。
- `TranslationService`：只对最终字幕执行翻译。
- `EventSink`：负责输出合法、完整的进程协议事件。
- `ProviderRegistry`：通过注册表选择 Provider，避免入口文件不断增长条件分支。

Provider 应遵循统一生命周期语义：

```text
start -> accept_audio(frame)* -> stop
```

识别结果必须区分：

- 中间字幕 `partial`：允许更新，禁止触发翻译。
- 最终字幕 `final`：固化一次，只触发一次翻译。
- 心跳：不得进入字幕记录。
- 错误：必须包含可诊断信息，但不得包含密钥。

### 8.2 Electron 主进程

进程管理至少应分离为：

- 引擎配置到命令参数的构建。
- 子进程生命周期与超时。
- stdout/stderr 解码。
- 本地 TCP 命令通道。
- 事件到窗口 IPC 的分发。

stdout 使用 NDJSON 时，解析器必须维护跨数据块缓冲区，正确处理：

- 一条 JSON 被拆成多个数据块。
- 多条 JSON 合并在一个数据块。
- 最后一行暂时不完整。
- 单条非法数据不破坏后续合法数据。

### 8.3 Vue 渲染进程

- 引擎界面应由 Provider 元数据和 capability registry 驱动。
- 通用文本、数字、选择、开关和密码字段使用通用表单渲染。
- 只有远端热词管理器等复杂交互使用专用组件。
- Pinia store 负责状态，不负责供应商协议和网络调用。
- 新增用户可见文本时，必须同时补齐中文、英文和日文，不得只完成一种语言。

## 9. 配置与类型约束

- 持久化配置必须包含 `schemaVersion`。
- 配置结构变化必须提供显式迁移函数，并测试旧配置升级。
- 不得依靠零散的 `if (config.xxx)` 实现长期迁移。
- Provider 通用配置和 Provider 专属配置必须分层。
- 类型、默认值、校验和 UI 元数据应尽量保持单一事实来源。
- 主进程必须验证来自渲染进程的配置，不得信任任意 IPC 数据。
- 新增配置必须说明默认值、合法范围、缺失行为和降级方式。
- 保存配置时不得意外覆盖未知但仍需兼容的字段。

推荐结构：

```json
{
  "schemaVersion": 2,
  "engine": {
    "provider": "fun_asr",
    "common": {},
    "providers": {
      "fun_asr": {}
    }
  }
}
```

## 10. 进程协议与兼容性

- 自定义字幕引擎是公开扩展点，协议变化必须优先考虑向后兼容。
- 未经明确的版本迁移方案，不得直接删除现有 `command` 消息格式。
- 新协议必须带版本或具有可可靠识别的事件 envelope。
- Python stdout 只能输出协议允许的数据；调试信息必须使用协议日志事件或 stderr。
- 每条 stdout 协议消息必须独占一行并立即刷新。
- 本地 TCP 命令也必须有明确消息边界，不能假设一次 `recv()` 等于一条完整 JSON。
- 协议字段变化必须同步更新 `docs/api-docs/` 和协议测试。

## 11. Fun-ASR 与热词功能约束

### 11.1 Fun-ASR 接入

- Python 中优先使用官方 DashScope SDK；只有 SDK 无法满足需求时才考虑裸 WebSocket，并在 `change.md` 记录理由。
- SDK 版本必须显式约束，不得依赖无上限、不可复现的最新版本行为。
- 必须处理 task started、partial、final、heartbeat、task finished 和 task failed。
- 翻译只在 final 事件后执行一次。
- 优先使用服务端句级/字级时间戳；不得把网络回调到达时间冒充精确音频时间。
- WebSocket 地址必须可配置，并验证 API Key、地域和 Workspace 的一致性。
- 必须提供停止时的结果冲刷和连接关闭逻辑。
- 生产路径需要有界重试、指数退避和明确的最终失败状态，禁止无限重连。

### 11.2 音频

- Fun-ASR 输入必须是单声道。
- 优先保留 Provider 支持的原始采样率，避免无必要的逐块重采样。
- 每帧建议约 100ms，并满足服务端单帧大小限制。
- `AudioFrame` 应携带采样率、声道数、格式和时间信息，禁止依赖隐式全局状态。

### 11.3 热词语义

必须明确区分：

- 预编译热词：通过 `vocabulary_id` 使用，可带权重，属于远端持久资源。
- 即时热词：随请求传递；不得假设 Fun-ASR 所有模型都支持。
- 上下文术语：用于上下文增强，不等同于加权热词。

禁止把上下文术语在 UI 或文档中错误宣传为“加权热词”。

### 11.4 热词远端资源

- 不得在每次启动字幕时自动创建或删除热词表。
- 创建、更新、删除远端热词表必须是用户明确触发的操作，并显示目标账号、地域、模型和资源 ID。
- 必须验证热词表目标模型与当前识别模型一致。
- 必须记录远端操作成功或失败，但不得记录 API Key。
- 删除热词表等不可逆远端操作必须二次确认。

## 12. 翻译服务约束

- 识别 Provider 不应复制翻译业务逻辑。
- 所有非自带翻译的 Provider 通过统一 `TranslationService` 调用 Ollama、OpenAI 兼容服务或 Google 翻译。
- 翻译任务必须关联稳定的字幕 ID，不得仅依赖可能碰撞的显示时间字符串。
- 翻译失败不得删除原字幕。
- 异步翻译结果晚到时必须正确关联原句，不能覆盖后续字幕。
- 对外部翻译调用必须有超时、错误分类和取消策略。

## 13. 安全约束

- API Key、Token 和密码不得写入日志、异常文本、测试快照、命令展示或 `change.md`。
- 日志脱敏必须覆盖所有新增密钥参数和配置字段。
- 新功能不得继续扩大明文凭据存储；涉及凭据改造时优先使用系统安全存储或环境变量。
- 渲染进程不得直接获得不必要的密钥。
- IPC handler 必须校验参数类型、长度和合法范围。
- 自定义引擎路径和参数不得通过 shell 字符串拼接执行；优先使用可控的可执行路径和参数数组。
- 外部 URL 只允许已知协议，并避免把不可信输入直接交给系统 shell。

## 14. 测试要求

### 14.1 必测范围

任何影响相应模块的修改，至少覆盖：

- 配置默认值和旧版本迁移。
- Provider 生命周期。
- partial 到 final 的字幕更新。
- final 只触发一次翻译。
- stdout 数据块边界。
- 启动超时、正常停止和强制停止。
- 热词参数选择与模型能力校验。
- API Key 和命令日志脱敏。

### 14.2 测试分层

- 单元测试不得依赖真实麦克风、扬声器或付费 API。
- 使用固定音频和伪造 Provider 做可重复测试。
- 真实阿里云 API 测试必须显式启用，使用环境变量提供凭据，默认 CI 不执行。
- 跨平台相关修改必须说明实际测试的平台；未测试的平台不得声称支持已验证。

### 14.3 基本验证命令

根据修改范围执行：

```bash
npm run typecheck
npm run lint
npm run build
```

Python 部分应执行项目当时配置的测试和静态检查。若项目尚未建立相应命令，应在新增核心抽象时同步建立，而不是长期依赖人工运行。

- 不得把“命令不存在”“依赖未安装”写成测试通过。
- 测试产生的缓存和构建产物不得误加入版本控制。
- 打包测试与普通构建分开记录，因为它可能依赖平台工具和预构建 Python 引擎。

## 15. 文档要求

以下变化必须同步更新相关文档：

- 新引擎、参数或使用方式：README 和用户手册。
- Python 引擎扩展机制：`docs/engine-manual/`。
- Electron IPC：`docs/api-docs/electron-ipc.md`。
- Python/Electron 进程协议：`docs/api-docs/caption-engine.md`。
- 用户可见行为：中文、英文、日文文档保持一致。
- 版本发布：`docs/CHANGELOG.md`，但不得用它替代根目录 `change.md`。

文档必须说明限制、地域、模型版本、费用或外部资源要求，不得只描述理想路径。

## 16. 依赖与构建产物

- 新依赖必须说明必要性、维护状态、许可证、平台兼容性和替代方案。
- 修改依赖时必须同步更新锁文件，并在 `change.md` 列出直接依赖变化。
- 不得直接编辑 `node_modules/`、`out/`、`dist/` 等生成目录。
- 不得把本地模型、音频样本、密钥文件或大型构建产物加入 Git，除非用户明确要求并确认许可。
- PyInstaller spec 变化必须验证目标平台的隐式依赖和资源打包。
- Electron 打包配置变化必须区分 Windows、macOS、Linux，不得假设一个平台路径适用于全部平台。

## 17. 代码风格与可维护性

- 遵循仓库现有 TypeScript、Vue、Python 和 Prettier/ESLint 配置。
- 命名表达领域含义，避免 `data1`、`temp2`、`handleStuff` 等模糊名称。
- 注释解释“为什么”和约束，不重复代码表面行为。
- 公共接口、协议事件和迁移函数必须有明确类型。
- 避免 `any`；必须使用时缩小边界并说明原因。
- 不捕获后静默忽略异常；必须分类处理、转换或记录。
- 不允许无界队列、无界线程创建和无限重试。
- 性能优化必须有测量依据，不能牺牲正确性换取未经验证的速度。

## 18. Git 与交付约束

- 不得覆盖用户未提交修改。
- 未经用户要求，不创建分支、提交、推送或 PR。
- 每次交付必须说明：
  - 完成了什么。
  - 修改了哪些文件。
  - 执行了哪些验证及结果。
  - 哪些验证没有执行及原因。
  - 已知风险和下一步。
- 如果修改成功但验证失败，必须明确标记为未完成或有风险，不得用“已完成”掩盖。

## 19. 完成定义

一项修改只有同时满足以下条件才算完成：

- 变更处于用户授权范围内。
- 结构符合上述职责边界，没有新增明显的条件分支堆积或重复实现。
- 配置、协议、迁移和兼容性已处理。
- 用户可见内容具有中英日三语支持。
- 相关测试已执行并记录真实结果。
- 相关文档已更新。
- 根目录 `change.md` 已追加完整记录。
- `git diff` 中不存在无关文件、生成文件、密钥或意外格式化。
